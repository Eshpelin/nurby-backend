"""Nurby MCP server package.

Re-exports the read-only agent tool registry over the Model Context
Protocol so any MCP client (Claude Desktop, etc) can query a
household's cameras. See ``services.mcp.server`` and docs/mcp.md.
"""
