# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
env/
venv/
.venv/
*.egg-info/
dist/
build/

# Environment
.env
.env.*
!.env.example

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Docker
docker-compose.override.yml

# Node (frontend)
node_modules/
.next/
out/

# Storage
/recordings/
thumbnails/
clips/

# Database
*.db

# ML models. vendored into images at build time via scripts/fetch-models.sh,
# never committed (hundreds of MB). a clean checkout runs the script first.
*.pt
*.onnx
*.pth
services/perception/models/

# Dev runtime
dev/logs/
dev/sample-cctv.mp4

logs/

# Agent eval artifact
.eval-report.md

# Claude Code. ship the shared dev-workflow agents/skills + launch config,
# but never the local settings, runtime locks, or session state.
.claude/*
!.claude/agents/
!.claude/skills/
!.claude/launch.json
.claude/settings.local.json
.claude/scheduled_tasks.lock

# macOS
.DS_Store

# Nested preview tooling
frontend/.claude/
